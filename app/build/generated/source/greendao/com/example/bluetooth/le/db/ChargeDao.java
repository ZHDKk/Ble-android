package com.example.bluetooth.le.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.bluetooth.le.db.bean.DaoSession;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CHARGE".
*/
public class ChargeDao extends AbstractDao<Charge, Long> {

    public static final String TABLENAME = "CHARGE";

    /**
     * Properties of entity Charge.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property EndTime = new Property(1, String.class, "endTime", false, "ENDTIME");
        public final static Property Voltage = new Property(2, String.class, "voltage", false, "VOLTAGE");
    }


    public ChargeDao(DaoConfig config) {
        super(config);
    }
    
    public ChargeDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CHARGE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"ENDTIME\" TEXT," + // 1: endTime
                "\"VOLTAGE\" TEXT);"); // 2: voltage
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CHARGE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Charge entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String endTime = entity.getEndTime();
        if (endTime != null) {
            stmt.bindString(2, endTime);
        }
 
        String voltage = entity.getVoltage();
        if (voltage != null) {
            stmt.bindString(3, voltage);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Charge entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String endTime = entity.getEndTime();
        if (endTime != null) {
            stmt.bindString(2, endTime);
        }
 
        String voltage = entity.getVoltage();
        if (voltage != null) {
            stmt.bindString(3, voltage);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Charge readEntity(Cursor cursor, int offset) {
        Charge entity = new Charge( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // endTime
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // voltage
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Charge entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setEndTime(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setVoltage(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Charge entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Charge entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Charge entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
